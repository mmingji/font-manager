# snfont 图标管理项目交接文档

> 版本：v1.0 ｜ 交接日期：2026 ｜ 基于最新提交：`f870b4e`

---

## 一、当前状态摘要

本项目是一个**纯前端、零后端**的**字体图标管理应用**：上传字体文件解析为 SVG、管理图标项目（增删改查/分组/批量操作）、一键生成 snfont 字体（ttf/woff/woff2）及配套 CSS，支持 GSUB 连字（输入图标名显示图标）。

**技术栈**：Vue 3 + Vite + Pinia + fonteditor-core + opentype.js + pinyin-pro（中文拼音分组）+ JSZip 打包。数据存 localStorage。

**运行方式**（离线、双击运行）：
- 双击 `启动服务.bat` → 起本地静态服务（`server.mjs`）→ 自动打开浏览器 http://localhost:2333；
- 双击 `关闭服务.bat` → 停止服务；
- 首次运行需已构建 `dist/`（`npm run build`）；若用我打包好的 dist 则无需编译。

---

## 二、已完成功能

| 功能 | 说明 |
|---|---|
| 字体解析 | 上传 ttf/otf/woff/woff2 → 解析为统一尺寸 SVG，支持预览/勾选/改名/下载 zip；保持原 unicode 默认开启；无名字字形按 uniXXXX 兜底命名；>10MB/字形>5000 提示不阻断；解析失败回显 |
| 图标管理 | 主页面按**名称首字母分组**（中文按**拼音首字母**，pinyin-pro），侧边字母导航（固定 A-Z→0-9→# 规范序）；卡片显示 unicode 码位，hover 显示改名/替换/删除 |
| 批量操作 | 多选：全选、批量删除、导出选中 SVG |
| 项目设置 | 项目名、CSS 前缀、字体名、**基础字符字重**（regular/bold）、SVG 尺寸；按 unicode 映射批量改名 |
| 项目下载 | 下载 zip（字体 ttf/woff/woff2 + css + demo.html + project.json），文件按产物名建文件夹（snfont-regular/ 或 snfont-bold/）|
| 字体能力 | GSUB 连字（输入图标名显示图标）、post 表字形名、稳定码位、ASCII 94 字符基础字形（双字重）+空格 |
| 顶部交互 | 按钮重排：`搜索｜多选｜设置｜导入▾(导入SVG/图片转SVG/解析字体)｜导出▾(下载项目/导出SVG)`；下拉三角为单色线性 svg |
| 空状态引导 | 首次打开提示"请先导入 SVG 或上传字体解析"（替代"没有匹配的图标"）|
| 图片转 SVG | **功能开发中占位**：弹窗显示"🚧 功能开发中"，暂无实际转换能力 |
| 离线运行 | server.mjs + 启动/关闭 bat，本地静态服务，双击运行 |

---

## 三、文件结构

```
snfont-manager/
├─ index.html            # 入口页(tag 固定"图标管理")
├─ vite.config.js        # Vite 配置(相对路径 base, 大包警告)
├─ package.json          # 依赖与脚本
├─ README.md             # 原有说明
├─ AGENTS.md             # 项目规范(每轮对话准则)
├─ server.mjs            # 本地静态服务(批处理用)
├─ 启动服务.bat          # 双击启动: 起服务+开浏览器
├─ 关闭服务.bat          # 双击停止服务
├─ .gitignore
├─ public/
│  ├─ unicode-map.json   # unicode→名称映射(官网 v7.3.1 提取)
│  └─ woff2.wasm         # woff2 解码 wasm
├─ docs/
│  └─ 手写字体制作软件-需求与技术方案.md  # 手写体方向说明(已单独成文)
└─ src/
   ├─ main.js
   ├─ App.vue           # 主页面(顶部交互/空状态/弹窗挂载)
   ├─ style.css
   ├─ store/project.js  # Pinia: CRUD/分组/稳定码位/项目配置
   ├─ components/
   │  ├─ FontParser.vue     # 解析字体弹窗
   │  ├─ ImportModal.vue    # 导入 SVG/项目文件弹窗
   │  ├─ ImageToSvgModal.vue # 图片转SVG弹窗(功能开发中占位)
   │  ├─ SettingsModal.vue  # 项目设置弹窗
   │  ├─ IconGrid.vue       # 图标分组网格+字母导航
   │  ├─ IconCard.vue       # 单个图标卡片
   │  └─ DropdownMenu.vue   # 下拉菜单(导入/导出)
   └─ lib/
      ├─ buildFont.js       # SVG→TTF/woff/woff2 构建
      ├─ baseGlyphs.js      # 内置拉丁字形(双字重+空格, 连字触发)
      ├─ gsub.js            # GSUB 连字表生成
      ├─ parseFont.js       # 字体→SVG 解析
      ├─ svgNormalize.js    # SVG 规范化(修复已规范判定bug)
      ├─ unicodeMap.js      # unicode→名称映射
      ├─ pinyin.js          # 中文拼音首字母分组/排序
      ├─ zip.js             # zip 打包/项目下载/demo生成
      └─ persist.js         # localStorage 持久化
```

---

## 四、剩余待办

1. **图片转 SVG 实际功能**：当前仅"功能开发中"占位弹窗。真正开发时难点是**矢量化**——imagetracerjs 对细描边线稿效果差（会填充成块），**potrace** 更合适但浏览器集成受 Jimp 依赖限制。建议后续用 potrace 浏览器化或 WASM 版。
2. **顶部下拉「导出▾→下载项目」样式**：当前"下载项目"是普通菜单项，可考虑视觉上强调为主操作。
3. **README.md 待同步**：README 还引用旧按钮结构（导入/解析字体等独立按钮），建议后续更新为下拉结构。
4. **数据迁移兼容**：localStorage 旧版本数据若缺 weight/新字段，store 已有默认值，基本兼容；如需旧项目json导入验证可测。
5. **矢量化质量**（如后续做图片转SVG）：照片/渐变/复杂细节差，仅适合纯色/线稿。

---

## 五、关键决策记录

| 决策 | 说明 |
|---|---|
| 字重方案 | 基础字符字重可选(regular/bold)，产物名 `snfont-regular`/`snfont-bold`，图标名/unicode/css 类名与字重无关 |
| 中文分组 | 图标名按拼音首字母(A-Z)分组，用 pinyin-pro；未写字/数字归 0-9、符号归 # |
| 排序修复 | `groupIcons` 固定 A-Z→0-9→# 分组顺序(不随数据乱序)，修复侧边导航 z 在 a 前的bug |
| SVG 归一化 | 修复"已规范"判定bug：bbox 宽高需≥900 才算已规范，否则缩放归一化(解决 imagetracerjs 像素坐标被漏缩放) |
| 映射数据 | unicode-map.json 用参考图标集 v7.3.1 官网数据覆盖(3726条)，未保留旧 v6 图标 |
| 运行形态 | 纯网页+批处理(离线双击运行)；未用 Electron/Tauri(避免 Win7 兼容问题) |
| 图片转SVG | 暂缓，标记"功能开发中"，imagetracerjs 对线稿局限，备选 potrace |
| puppeteer-core | 归 devDependencies(测试/调试用，生产不装)；误删可 `npm i -D puppeteer-core` 重装 |
| 删除了 imagetracerjs | 图片转SVG功能留待，依赖已移除 |

---

## 六、如何跑起来

```bash
# 开发
npm install      # 装依赖
npm run dev      # 打开 http://localhost:5173

# 生产(离线双击运行)
npm run build    # 生成 dist/
# 然后双击「启动服务.bat」→ 自动开 http://localhost:2333
```

> 若用打包好的 dist，无需 npm run build，直接双击「启动服务.bat」即可。

---

*交接文档基于当前 git 提交 `f870b4e`（含此前 0180fbe 修复与 6252970 初始提交）。*
