# snfont 图标管理项目交接文档

> 版本：v1.1 ｜ 交接日期：2026 ｜ 基于最新提交：`b8bc35d`

---

## 一、当前状态摘要

纯前端、零后端**字体图标管理应用**：上传字体解析为 SVG、图标项目管理、生成 snfont 字体(ttf/woff/woff2)+CSS，支持 GSUB 连字。

**技术栈**：Vue 3 + Vite + Pinia + fonteditor-core + opentype.js + pinyin-pro + JSZip。

**运行方式**（离线双击）：`启动服务.bat` → `server.mjs` 起静态服务 → http://localhost:2333；`关闭服务.bat` 停止。首次需 `npm run build` 生成 dist/。

---

## 二、已完成功能

| 功能 | 说明 |
|---|---|
| 字体解析 | ttf/otf/woff/woff2 → 统一尺寸 SVG；虚线框上半=解析前设置(尺寸/映射/保持unicode)，下半=文件区(已解析+点击重选)；预览改名/勾选/下载；unicode-map.json 自动命名 |
| 码位冲突检测与处理 | 冲突 = 原码位与内置 ASCII 基础字形(0x20-0x7E) 或项目已有图标码位相同 → 卡片标红 + 三选一（自动分配/移除冲突/覆盖旧图标）；有冲突必须三选一才能导入；「覆盖」可替换内置基础字形(ASCII)或同码位旧图标(保留其名) |
| 图标管理 | 首字母分组(中文按拼音)、侧边字母导航(A-Z→0-9→#)、改名/替换/删除、多选批量 |
| 项目设置 | **右侧抽屉**：项目名/CSS前缀/字体名/字重/SVG尺寸 + 映射批量改名 + 码位占用统计(动态刷新) |
| 项目下载 | zip：ttf/woff/woff2+css+demo.html+`<项目名>.project.json`，按产物字体名建文件夹 |
| 数据持久化 | **IndexedDB 主存储** + localStorage 快照；写队列串行 + pagehide 同步兜底；boot gate 防大数据空白闪现 |
| 启动自检 | probeAbnormal 按需检测越界 SVG → 确有异常才提示修复(进度+数量,5s消失)；幂等，正常数据静默 |
| 顶部交互 | 搜索｜多选｜设置｜导入▾｜导出▾；导入/导出下拉**互斥**；导出菜单右对齐 |
| 码位规划 | 新增图标 U+EE00–U+EFFF(512)顺序分配；allocateCode 双向跳过已占用；ASCII 覆盖由 buildFont 跳过内置 |
| 图片转 SVG | 占位「功能开发中」，待后续 |

---

## 三、文件结构

```
snfont-manager/
├─ index.html / vite.config.js / package.json / README.md
├─ AGENTS.md             # 项目规范(每轮对话准则)
├─ server.mjs            # 本地静态服务(批处理用)
├─ 启动服务.bat / 关闭服务.bat
├─ public/
│  ├─ unicode-map.json    # unicode→名称映射(参考图标集 v7.3.1)
│  ├─ codepoint-plan.json # 码位规划文档基线(占用统计为动态)
│  └─ woff2.wasm
├─ scripts/              # verify-full-cmds / test-fonteditor-build 验证脚本
└─ src/
   ├─ main.js / App.vue / style.css
   ├─ store/project.js   # Pinia：CRUD/分组/稳定码位/冲突覆盖/overwriteByCode/repairAll(进度)/bootstrap
   ├─ components/
   │  ├─ FontParser.vue     # 解析字体(虚线框布局+冲突三选一)
   │  ├─ ImportModal.vue    # 导入 SVG/项目文件
   │  ├─ ImageToSvgModal.vue# 占位
   │  ├─ SettingsModal.vue  # 设置抽屉(映射改名+码位占用)
   │  ├─ IconGrid/IconCard/DropdownMenu.vue
   └─ lib/
      ├─ buildFont.js / baseGlyphs.js / gsub.js / parseFont.js
      ├─ svgNormalize.js  # SVG 规范化(幂等)
      ├─ unicodeMap.js / codepointPlan.js / codepointStats.js / pinyin.js
      ├─ zip.js / persist.js  # persist: IndexedDB 主存储
```

---

## 四、剩余待办

1. **图片转 SVG 实际功能**：当前仅占位。矢量化难点：imagetracerjs 对细描边线稿差(填充成块)，potrace 更合适但浏览器集成受 Jimp 依赖限制，建议 potrace 浏览器化或 WASM 版。
2. **矢量化质量**：照片/渐变/复杂细节差，仅适合纯色/线稿。

---

## 五、关键决策记录

| 决策 | 说明 |
|---|---|
| 字重方案 | regular/bold 两套产物文件名，图标名/unicode/css 类名与字重无关 |
| 中文分组 | pinyin-pro 拼音首字母(A-Z)；未写字/数字归 0-9、符号归 # |
| 分组排序 | groupIcons 固定 A-Z→0-9→# 规范序(不随数据乱序) |
| 码位保留区 | 新增图标 U+EE00–U+EFFF 顺序分配(512)，避开参考字体已占 E000-E8CC/F000+；nextCode 只增不减(删除不释放) |
| ASCII 冲突策略 | 解析导入与内置基础字形冲突 → 预览三选一；覆盖=用户字形替换内置(buildFont 跳过被覆盖码位) |
| SVG 幂等 | normalizeSvgImport 判定「bbox 在 0~1000 且全大写」即规范不改写，修复收敛(不每轮重复提示) |
| 持久化 | IndexedDB 主存储(容量大) + localStorage 快照；JSON 深拷贝防 DataCloneError；写队列串行；pagehide 兜底 |
| 启动恢复 | bootstrap + booted gate：先 IDB hydrate 再渲染，防大项目先空白后闪现 |
| 下拉互斥 | DropdownMenu 受控(defineModel open)，父组件协调导入/导出同时只开一个 |
| 映射数据 | unicode-map.json = 参考图标集 v7.3.1 官网数据(4331 条含品牌) |
| 运行形态 | 纯网页+批处理离线运行；未用 Electron/Tauri(避免 Win7 兼容) |
| puppeteer-core | devDependencies(测试用)；误删可 npm i -D puppeteer-core 重装 |
| 图片转SVG | 暂缓，标记「功能开发中」 |

---

## 六、如何跑起来

```bash
npm install
npm run dev     # http://localhost:5173
npm run build   # dist/
双击 启动服务.bat  # http://localhost:2333
```
